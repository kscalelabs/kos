/**
 * Defines state variables that are passed between the runner loop and
 * the calling process.
 */

#[derive(Debug)]
pub struct State {}

impl State {
    pub fn new() -> Self {
        State {}
    }
}
